// Placeholder route for the center "Create" tab. The trigger is disabled in
// the tab layout, so this screen is never shown — pressing the tab opens the
// create menu overlay instead.
export default function CreateScreen() {
  return null;
}
