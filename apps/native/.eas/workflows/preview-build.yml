name: Native Preview Build

defaults:
  tools:
    node: 22.11.0
    corepack: true
    pnpm: 11.9.0

on:
  pull_request:
    branches:
      - dev
      - main
    paths:
      - apps/native/api/**
      - apps/native/app/**
      - apps/native/assets/**
      - apps/native/components/**
      - apps/native/content/**
      - apps/native/global.css
      - apps/native/gt-lock.json
      - apps/native/hooks/**
      - apps/native/lib/**
      - apps/native/loadTranslations.ts
      - apps/native/polyfills/**
      - apps/native/providers/**
      - packages/**
      - tsconfig.json
      - "!**/*.md"

concurrency:
  cancel_in_progress: true
  group: ${{ workflow.filename }}-${{ github.ref }}

jobs:
  validate:
    name: Validate Workspace
    steps:
      - uses: eas/checkout
      - uses: eas/install_node_modules
      - name: Check types
        run: pnpm --dir ../.. check-types

  build_android:
    name: Build Android Preview
    needs: [validate]
    type: build
    environment: preview
    params:
      platform: android
      profile: preview
      message: Preview build from ${{ github.ref_name }}

  build_ios:
    name: Build iOS Preview
    needs: [validate]
    type: build
    environment: preview
    params:
      platform: ios
      profile: preview
      message: Preview build from ${{ github.ref_name }}
