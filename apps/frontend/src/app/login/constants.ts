/**
 * Milliseconds between testimonial rotations on the login/register page.
 */
export const TESTIMONIAL_ROTATION_MS = 5000;

/**
 * Milliseconds the testimonial takes to cross-fade before the next one is
 * mounted.
 */
export const TESTIMONIAL_FADE_MS = 400;
